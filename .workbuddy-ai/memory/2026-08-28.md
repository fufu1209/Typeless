# 2026-08-28

## Typeless Switchboard v2 全面落实（第一轮）

- 现状盘点：`main-fb72c3e3` 干净停在 v1.1.0（commit `33ed9f1`）。
  之前聊天里说"已经做了"的 `docs/v2-upgrade-plan.md` / `QuotaCycleEngine`
  / `.workbuddy-ai/memory/2026-08-28.md` 实际**未落盘**（很可能是被 worktree 切换
  或本地未提交改动覆盖）。本轮起每次变更都基于 worktree 真实状态。
- 关键证据：
  - P0-1 `main.swift:5386` 硬编码 `/Users/fucaixie/BC/Typeless/...` 仍在
  - P0-2 `main.swift:419-424` 仍是 `try?` 静默吞错误 + 立即落盘
  - `extract-active-session.js:155` 确认是 `week_word_usage_value` / `week_word_usage_limit`（周额度）
  - `OperationalModels.swift:1564-1710` 的 `SmartSwitchPolicy` 没有 `daysUntilReset` / `detectRevival` / `pickNext`
- 本轮交付：
  1. `docs/v2-upgrade-plan.md`：6 阶段（数据安全 / 周度复活 / UI 重构 / 图标 / 测试改革 / 架构拆分）+ 多 worktree 并行计划
  2. `Sources/TypelessSwitchboardCore/QuotaCycleEngine.swift`：纯函数 `daysUntilReset` / `shouldRevive` / `revivedAccounts` / `pickNext`，不依赖 main.swift 里的 Account
  3. P0-1 修复：删 `main.swift:5386` 硬编码用户路径
  4. P0-2 修复：`main.swift:419-424` 改成 `do/catch` + 损坏文件备份为 `store.json.corrupted-<时间戳>` + 新增 `accountLoadError` 给 UI 展示
- 下轮目标：把 QuotaCycleEngine 接到 `SmartSwitchPolicy.decide` 和
  `AccountStore.smartSwitchCandidates`，把"周一自动复活回账号一"真正跑通；
  接着 UI 5 tab 重构、图标、测试改革、架构拆分。
- "100% 强大稳定"硬指标见 v2-upgrade-plan.md §8。

## v2.4.0 测试改革完成（commit e746216，worktree v24-test-real）
- 删除 256 行 / 108 条源串包含断言（`switchboardSource.contains("func xxx")` 这种
  测「符号在不在」而非「行为对不对」，函数体清空照样通过）
- 新增 6 个真实行为测试模块，断言总数 262：
  runSmartSwitchPolicyChecks / runRegistrationCompletionPolicyChecks /
  runBrowserAutomationResultPayloadChecks / runToolkitAccountImporterEdgeCaseChecks /
  runStoreRecoveryChecks / runQuotaGuardLaunchAgentPlannerChecks
- 新增 Core 模块 `StoreRecovery.swift`：把 P0-2 损坏恢复逻辑从 App target 抽到 Core，
  main.swift 改用 StoreRecovery.load（行为一致）
- 验证 Agent 3 遗留的 `QuotaGuardLaunchAgentPlanner.swift`：main.swift 5 处调用，非死代码

## v2.2.0 UI 5-tab 重构（worktree v22-ui-reshape，进行中）
- 骨架已提交 2ba9f23，swift build 通过
- 5 个 tab：账号池 / 智能换号 / 额度守护 / 注册与邮箱 / 自检排障
- **SwiftPM 大坑**：可执行 target 里 `main.swift` 的顶层代码与 `@main` 属性互斥。
  单文件时能编译，一旦拆出第二个文件就报
  `'main' attribute cannot be used in a module that contains top-level code`
  → 解法：实现迁到 AppMain.swift，main.swift 只留一行 `TypelessSwitchboardMain.main()`，
  去掉 `@main`，`static func main()` 显式标 `@MainActor`
- 另一个坑：`Color.tertiary` 是 ShapeStyle 不是 Color，要用 `Color(nsColor: .tertiaryLabelColor)`
- SwiftUI 写法坑：`SettingsCard(title:symbolName:) { content } footnote: { }` 编译不过
  （尾随闭包后不能再跟标签参数）→ footnote 必须是 String 且放在尾随闭包**之前**

## v2.2.0 UI 5-tab 重构完成（commit 0e92dce）
5 个 tab：账号池 / 智能换号 / 额度守护 / 注册与邮箱 / 自检排障
顶部常驻状态带（额度摘要 + 错误横幅 + 一键换号）
功能等价性核对：旧实现 65 个 store 成员，新 UI 覆盖 65 个，丢失 0
AppMain.swift 删 1088 行旧 UI（LegacyContentView + InspectorView）

## v2.5.0 架构拆分完成（commit c6e88ef）
main.swift 7544 行 → 28 个职责文件：
  main.swift(8) / Model(2) / Store(6) / Support(3) / App(3) / UI(14)
完整性核对：153 方法名 / 155 条声明与单文件基线一致；143 唯一签名重复 0

### 拆分踩坑清单（下次直接照做）
1. **SwiftPM @main 互斥**：可执行 target 有 main.swift 时，顶层代码与 @main 不能共存。
   单文件能蒙过，多文件必报 `'main' attribute cannot be used in a module that contains top-level code`。
   → 实现搬到 AppMain.swift，main.swift 只留一行调用；入口 enum 去掉 @main，
     `static func main()` 显式标 @MainActor
2. **切分带属性**：`// MARK` 前的 `@MainActor` 会被留在上一段尾部 → 回移到下一段开头
3. **存储属性不能进 extension**（lastSavedStateData、6 个权限探测缓存）→ 移回主类体
4. **区间重叠会重复声明** → 切完必须按「方法签名」查重
5. **private/fileprivate 跨文件不可见**（`private extension String.ifEmpty`、
   `private extension JSONEncoder`）→ 提升为 internal
6. **切割后务必先合成单文件编译验证**，确认内容无损再正式拆
7. `Color.tertiary` 是 ShapeStyle 不是 Color → 用 `Color(nsColor: .tertiaryLabelColor)`
8. SwiftUI 尾随闭包后不能再跟标签参数：`SettingsCard(...) { content } footnote: {}` 编译不过，
   footnote 必须是 String 且放在尾随闭包之前

## v2 六阶段全部完成
commit 序列：9abe890(v2.0/v2.1) → 236f0b6(v2.3) → e746216(v2.4) → 0e92dce(v2.2) → c6e88ef(v2.5)
遗留：Store+Accounts(1677) 与 Store+LaunchAgent(1930) 仍偏大；UI 截图留档未做

### 最终复验（在 c5cbc6a 之上，全绿）
- swift build 零 error 零 warning
- OperationalFeatureChecks passed / AutomationSmokeChecks passed / test-operational-features.sh passed
- build-app.sh 打包 + 签名成功
- 断言 262 条（函数名是 `check(`，不是 `expect(`），switchboardSource 源串包含残留 0
- **重要**：本工程 `swift test` 无效（测试用例是可执行 target 的 main.swift，不是 XCTest），
  必须走 `swift run OperationalFeatureChecks` / `swift run AutomationSmokeChecks` / `bash scripts/test-operational-features.sh`
- 图标资源落点：仓库根 `Resources/AppIcon.icns`（1.2 MB，7 档），源码 `templates/AppIcon.svg`，生成脚本 `scripts/generate-icon.sh`

## v2.5.1 Store 二次拆分 + 功能保真审计（commit cd61ada，已 push 到 origin/main）
- `SwitchboardStore+Accounts`(1677) → 8 个文件；`+LaunchAgent`(1930) → 7 个文件
  Store 最大文件 1930 → 720 行；全工程 47 个 swift 文件 / 10959 行
- 验证：成员数守恒 63/63、75/75；**逐行内容比对 1747 / 1502 行完全一致**
- 功能保真审计（基线 v1.1.0 = 33ed9f1）：
  store 成员 64→70 丢失 0；控件 30→30 完全一致；按钮文案零丢失；未挂载 UI 组件 0
- build-app.sh 升版本号 2.0.0，新增 `--install` / `--launch`
- 已 push：`git push origin HEAD:main`（33ed9f1..cd61ada）

### Swift 大文件拆分踩坑（补充 v2.5.0 那条清单）
9. **不要用大括号匹配定函数边界**。函数体内若有字符串（尤其 JS/AppleScript 模板里的括号），
   括号计数失衡 → 函数范围算错、吞掉后面的函数。本轮就吞了 `openLastAutomationBrowserSession`。
   → 改用「成员起始行划区间」：区间 = 起始行 → 下一个成员起始行 − 1。绝不漏、绝不重叠。
10. **回吸注释/属性后，区间起点要用回吸行，不是签名行**，否则切掉 `///` 和 `@discardableResult`
11. **修饰符要列全**：`nonisolated` / `static` / `mutating` / `override` / `class` / `final` …
    漏了 `nonisolated` 会漏识别 `nonisolated static func`（本轮漏了 11 个）
12. **extension 内第一个成员之前的 `// MARK:` 不属于任何成员**，需单独补回
13. 拆分后必须做**逐行内容比对**，"方法名数量守恒"不足以证明内容没丢
14. 拆分脚本在 `/tmp/`（split_by_lines / split_groups / verify_split / extract-ui），
    **建议后续移入 scripts/ 入库**，目前未纳入版本控制

### 运行时注意
- 两个实例同时跑会抢同一个 `~/Library/Application Support/TypelessSwitchboard/store.json`
  （用户的旧副本在 `/Users/fucaixie/BC/Typeless/`，PID 694）
- `open` 命令检测同 bundle id，若已有实例运行只会激活旧的、不会启动新的 →
  验证新版本前必须先 `pkill -f` 再手动拉起二进制
- `screencapture` 无屏幕录制权限，截不了图 → UI 只能靠用户截图反馈
- `swift test` 在本工程无效（测试是可执行 target 的 main.swift）

## v2.5.2 用户截图反馈处理（commit b7df66b，已 push）
用户贴了 3 张图 + 一系列问题，逐个落实：

**1) 钥匙串弹窗太烦**
- 根因：ad-hoc 签名 + 没"始终允许"，每次都问
- 修复：KeychainStore 内部加进程级缓存（cachedAPIKey / cachedPasswords），
  首次读后内存返回，不再触发 SecItemCopyMatching。
  弹窗从「每次都问」降到「启动 1 次」
- 加 kSecAttrAccessibleAfterFirstUnlock（旧条目先删再建）
- Swift 6 并发：nonisolated(unsafe) + NSLock

**2) 第 5 个 tab「自检排障」被裁切**
- 根因：minWidth=1180 + 顶部右侧两个按钮，5 tab 装不下
- 修复：1180 → 1280

**3) 状态栏监听「应该还是之前的吧」**
- 用户怀疑对：v2 重构没丢，状态栏在 AppDelegate.swift 完整保留，
  用 NSStatusBar API（不是 SwiftUI MenuBarExtra），功能比旧版还强
  （菜单栏显示剩余字数、`?↓` 提示数字陈旧、⌘O/⌘R/⌘Q 快捷键、唤醒监听）

**4) 字数 < 200 边界**
- 加 runThresholdBoundaryChecks：199/200/201 边界、isApproachingQuotaLimit、
  nextCheckDelaySeconds、normalizeThreshold、状态机单调性
- 断言数 286 → 307

**5) 换 Mac 导入 / 公开私密双版本**
- 新增 ConfigurationBundle（schemaVersion=2）：
  - exportFullBundle → ~/Downloads/bundle-full-时间戳.json（含真实数据）
  - exportPublicBundle → ~/Downloads/bundle-public-时间戳.json（脱敏，可分享）
  - importFullBundle(from: URL) → 按邮箱去重合并
- 脱敏规则：邮箱 → demo[N]@example.com、notes 清空
- 不含 keychain（API Key / 账号强密码）—— 跨设备带不走，要在新机单独配
- 文档 docs/github-dual-repo-strategy.md：推荐「单 repo + 两类 Release」
  而非两个独立 repo（同步成本、贡献者体验考虑）

## v2.5.3 用户回归报障修复（commit aa89300 + 134ec8e）

用户 6 问：新手引导回归 / 下次可用不显示 / 一键换号应无感 / 账号池状态 /
8000 是什么 / 电脑里 4 个一样 app。

### 根因（三个叠加）
1. **onboarding 补丁 fail-closed**：邮箱 ≠ 期望邮箱就整段 return，两个文件一个都不写
2. **只挂在全自动注册路径**：全工程仅 AutomaticReplacement.swift:265 一个调用点，
   无感/智能/自动/手动切换四条路径都不打补丁
3. **Typeless 2.4.0 换 schema**：onboarding 平台枚举 4 → 7
   （+ linux/harmony/webpage），macos 节点新增 app_version/completed_at

### 真相（实测，写进 docs/onboarding-patch-truth.md）
- `app-onboarding.json`（isCompleted/step）是**引导向导真正的开关**，
  本地改得动；Typeless 重启是「合并」不是「重置」，跨两次重启稳定保持 ✅
- `app-storage.json` 的 `userData.is_new_user` 是**服务端真值**，
  每次联网同步都被覆盖，本地压不住、也不该硬压
- **教训**：验证必须跨越被测对象的重启。第一次验证读的是覆盖前的文件 → 假阳性

### 改动
- OnboardingPatch.swift 重写为 fail-safe：按实际登录邮箱打补丁，
  平台列表 = 文件已有键 ∪ 7 官方枚举（未来加平台自动兼容）
- 新增 `writeTypelessDesktopOnboardingFiles`（纯文件写入，无进程控制）：
  无感换号在 `reinjectSessionPayload` 里「写会话之后、拉起 Typeless 之前」调用
- 新增 `skipTypelessDesktopOnboardingNow()` + CLI `--skip-onboarding`
- `QuotaCycleEngine` 新增 countdownText / nextAvailabilityText / nextResetDate（可单测）
- 新增 `Model/Account+QuotaCycle.swift`：Account↔快照唯一桥接点
- 单实例锁（flock）：GUI 抢占，daemon/CLI 放行。实测双开仍 1 进程
- build-app.sh 产物改到 ~/Library/Caches/TypelessSwitchboard，不再落仓库目录
  （.gitignore 挡得住 git 挡不住 Spotlight，这是「4 个一样 app」的真因）
- UI 口径 月→周 统一

### 数字
- 测试断言 307 → **335**（+runNextAvailabilityChecks 17 条、+runOnboardingSchemaChecks 11 条）
- 账号池：18 个账号，全部 available+approved，16 个带静默会话缓存
- 8000 = 每账号**每周**字数额度（非账号数）

### 遗留
- `/Users/fucaixie/BC/Typeless/TypelessSwitchboard.app` 是旧版副本，
  Spotlight 仍会搜到，等用户决定是否删除
